# Практична робота №1. Базові конструкції C#. Консольний застосунок

**Модуль:** 1 — огляд та повторення C#
**Тип застосунку:** консольний (.NET, Console App)
**Обсяг:** 2 год аудиторних + СРС

## Мета роботи

Закріпити навички роботи з базовими типами даних, операторами, керуючими конструкціями (розгалуження, цикли), масивами та методами мови C#; навчитися організовувати консольний діалог введення/виведення даних та структурувати найпростіший застосунок у вигляді меню з циклічним виконанням пунктів.

## Загальні завдання (обов'язкові для будь-якого варіанта)

- Створити консольний проєкт (.NET 10, Console App) у Visual Studio / Rider / VS Code з назвою виду `Lab1_<Прізвище>`.
- Реалізувати введення вихідних даних з клавіатури (консолі) з перевіркою коректності введення (обробка невірного формату без аварійного завершення програми — метод `TryParse` та цикл повторного запиту). **Виняток:** основний набір даних (наприклад, список студентів, товарів, температур) дозволяється спочатку заповнити наперед у коді, щоб не вводити все з нуля щоразу під час демонстрації. Але цей набір при цьому має лишатися *робочим*, а не «мертвим»: передбачте в меню пункт, який дозволяє змінювати дані під час роботи програми — додати новий елемент (студента, товар тощо) або, якщо розмір набору фіксований за змістом варіанта (наприклад, рівно 7 днів тижня), відредагувати значення одного з наявних елементів. Саме тут і демонструється введення з клавіатури з валідацією через `TryParse`. Для зберігання даних можна використовувати як звичайний масив (тоді додавання елемента робиться через `Array.Resize`), так і `List<T>` (тоді додавання — це просто виклик `Add`, а сам список все одно можна передавати в цикли `foreach` та власні методи так само, як масив) — студент обирає зручніший для себе варіант.
- Реалізувати обчислення/оброблення даних відповідно до свого варіанта завдання (конкретні вимоги — у таблиці варіантів нижче), використовуючи щонайменше:
  - умовний оператор або `switch` (statement або expression);
  - цикл (`for`/`while`/`foreach`) — там, де це доцільно за логікою варіанта;
  - одновимірний або двовимірний масив (якщо для основного набору даних обрано `List<T>` замість масиву — цей пункт вважається виконаним; масив/`List<T>` тут взаємозамінні);
  - щонайменше 2 власні методи (з параметрами та поверненим значенням, а не `void`-методи, що лише друкують у консоль).
- Реалізувати вивід результатів у зручному для читання вигляді (заголовки, підписи значень, інтерпольовані рядки). Табличний вивід з вирівнюванням стовпців (як у прикладі `PrintTable` нижче) не є обов'язковим, але вважається гарним тоном оформлення і вітається.
- Передбачити просте меню (цикл із вибором пункту через `switch`), що дозволяє повторювати обчислення без перезапуску програми, аж до вибору пункту «Вихід».
- Обробити щонайменше один граничний/особливий випадок свого варіанта (порожній масив, ділення на нуль, від'ємне значення там, де воно недопустиме тощо) і продемонструвати коректну поведінку програми в такому випадку.

## Теоретичні відомості (стисло)

Типи даних (value/reference types), літерали, оператори та їх пріоритет, конструкції розгалуження (`if`/`else`, `switch`/`switch expression`), цикли (`for`, `while`, `do-while`, `foreach`), масиви (одновимірні, двовимірні, «зубчасті» — `jagged`), методи та їх сигнатури, передача параметрів (за значенням / за посиланням, `ref`/`out`), базові засоби введення/виведення (`Console.ReadLine`/`Console.WriteLine`), форматування рядків (інтерполяція `$"..."`, композитне форматування, специфікатори вирівнювання в фігурних дужках).

## Довідник: обов'язкові до використання методи та ключові слова

Коротка довідка з посиланнями на офіційну документацію Microsoft Learn — не обов'язково читати все, достатньо розділу, що стосується вашого варіанта.

| Метод / ключове слово | Коротко про призначення | Посилання (MS Learn) |
|---|---|---|
| `int.TryParse` / `double.TryParse` | Безпечне перетворення рядка в число: повертає `true`/`false` замість викидання винятку при некоректному вводі — основа валідації в цій роботі | https://learn.microsoft.com/en-us/dotnet/api/system.int32.tryparse |
| `if` / `else` | Базове розгалуження виконання коду за умовою | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/statements/selection-statements#the-if-statement |
| `switch` (statement) / `switch expression` | Розгалуження за кількома варіантами значення; `switch expression` — компактніший синтаксис, що повертає значення | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/statements/selection-statements#the-switch-statement · https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/operators/switch-expression |
| `for`, `while`, `do-while`, `foreach` | Керуючі конструкції циклів — вибір залежно від того, чи відома кількість ітерацій наперед | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/statements/iteration-statements |
| Масиви (`int[]`, `int[,]`, `string[]`) | Одновимірні та багатовимірні масиви фіксованого розміру — базова структура даних для зберігання набору однотипних значень | https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/arrays/ |
| Методи (оголошення, параметри, повернене значення) | Іменований блок коду, що приймає параметри та (за потреби) повертає результат — основа декомпозиції програми | https://learn.microsoft.com/en-us/dotnet/csharp/programming-guide/classes-and-structs/methods |
| `ref` / `out` параметри | Передача аргументів методу за посиланням — метод може змінити значення виклику або повернути додатковий результат через `out` | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/ref · https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/out |
| Інтерпольовані рядки `$"..."` | Вбудовування значень змінних та виразів прямо в рядковий літерал, у т.ч. зі специфікаторами вирівнювання/формату (`{value,width:format}`) | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/tokens/interpolated |
| Композитне форматування рядків | Синтаксис `{значення,±ширина:формат}` для керування вирівнюванням (додатне число — праворуч, від'ємне — ліворуч) і форматом числа/дати при виводі (`:F2`, `:N0`, `:C`, `:P1` тощо); докладний розбір із прикладами — у розділі «Приклади коду» нижче | https://learn.microsoft.com/en-us/dotnet/standard/base-types/composite-formatting |
| Стандартні рядки числового формату (`F`, `N`, `C`, `P`, `D`, `X`) | Готові позначення для найпоширеніших способів показати число: `F2` — фіксована кількість знаків після коми, `N0` — з розділювачем тисяч, `C` — у грошовому форматі поточної культури, `P1` — у відсотках, `D3` — ціле число з провідними нулями, `X` — у шістнадцятковій системі | https://learn.microsoft.com/en-us/dotnet/standard/base-types/standard-numeric-format-strings |
| `string.Split`, `string.Trim`, `string.ToUpper`/`ToLower` | Базові рядкові методи: розбиття рядка на частини за роздільником, видалення пробілів по краях, зміна регістру — знадобляться у варіантах з текстом | https://learn.microsoft.com/en-us/dotnet/api/system.string.split · https://learn.microsoft.com/en-us/dotnet/api/system.string.trim |
| `Array.Sort`, `Array.Reverse` | Вбудоване сортування масиву за зростанням та розворот порядку елементів — можна використовувати як базу, якщо варіант не вимагає власного алгоритму сортування | https://learn.microsoft.com/en-us/dotnet/api/system.array.sort · https://learn.microsoft.com/en-us/dotnet/api/system.array.reverse |
| `Array.Resize` | Зміна розміру вже створеного масиву (створює новий масив потрібної довжини й копіює елементи) — знадобиться, щоб додати новий елемент до масиву, заповненого наперед у коді | https://learn.microsoft.com/en-us/dotnet/api/system.array.resize |
| `List<T>` (`Add`, `RemoveAt`, індексатор) | Узагальнена колекція зі змінним розміром — альтернатива масиву, якщо потрібно часто додавати/видаляти елементи без ручного виклику `Array.Resize`; у циклах і власних методах використовується практично так само, як масив | https://learn.microsoft.com/en-us/dotnet/api/system.collections.generic.list-1 |
| `Math.Round`, `Math.Max`, `Math.Min`, `Math.Abs` | Стандартні математичні операції з класу `Math` — округлення, порівняння, модуль числа | https://learn.microsoft.com/en-us/dotnet/api/system.math |
| `Console.ReadLine` / `Console.WriteLine` / `Console.Write` | Читання рядка з консолі та виведення тексту (з переходом на новий рядок або без) | https://learn.microsoft.com/en-us/dotnet/api/system.console |
| `throw` та стандартні винятки (`InvalidOperationException`, `ArgumentOutOfRangeException`) | Явне повідомлення про помилкову ситуацію в методі — знадобиться для обробки граничного випадку (п. «Загальні завдання») | https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/throw · https://learn.microsoft.com/en-us/dotnet/api/system.invalidoperationexception |

## Приклади коду (для орієнтиру — не є готовим розв'язком варіанта)

Наведені фрагменти ілюструють технічні прийоми, потрібні для роботи; логіку конкретного варіанта студент розробляє самостійно.

### 1. Валідне введення числа через `TryParse` і цикл повторного запиту

```csharp
static int ReadInt(string prompt, int min = int.MinValue, int max = int.MaxValue)
{
    while (true)
    {
        Console.Write(prompt);
        string? input = Console.ReadLine();

        if (int.TryParse(input, out int value) && value >= min && value <= max)
        {
            return value;
        }

        Console.WriteLine($"Некоректне значення. Введіть ціле число від {min} до {max}.");
    }
}
```

### 2. Метод із параметрами та поверненим значенням (обробка масиву)

```csharp
static double CalculateAverage(int[] values)
{
    if (values.Length == 0)
    {
        throw new InvalidOperationException("Масив порожній — обчислити середнє неможливо.");
    }

    int sum = 0;
    foreach (int v in values)
    {
        sum += v;
    }

    return (double)sum / values.Length;
}
```

### 3. Форматований табличний вивід у консоль (необов'язково, приклад гарного оформлення)

```csharp
static void PrintTable(string[] names, int[] scores)
{
    Console.WriteLine($"{"Студент",-15}{"Бал",5}");
    Console.WriteLine(new string('-', 20));

    for (int i = 0; i < names.Length; i++)
    {
        Console.WriteLine($"{names[i],-15}{scores[i],5}");
    }
}
```

**Як це працює: синтаксис вирівнювання й форматування в інтерпольованому рядку.**

Усередині фігурних дужок `{ }` в інтерпольованому рядку (`$"..."`) можна вказати не лише саме значення, а й додаткові вказівки через кому та двокрапку:

```
{ значення , вирівнювання : формат }
```

- **`значення`** — будь-який вираз C# (змінна, виклик методу, літерал);
- **`,вирівнювання`** (необов'язково) — ціле число, що задає мінімальну ширину поля в символах:
  - **додатне** число (наприклад, `,5`) — вирівнювання **праворуч**, поле доповнюється пробілами зліва;
  - **від'ємне** число (наприклад, `,-15`) — вирівнювання **ліворуч**, поле доповнюється пробілами справа;
  - якщо саме значення довше за вказану ширину — воно просто виводиться повністю, обрізання не відбувається;
- **`:формат`** (необов'язково) — рядок формату для чисел, дат тощо (наприклад, `:F2` — два знаки після коми, `:N0` — розділювач тисяч без дробової частини, `:C` — грошовий формат, `:P1` — відсоток з одним знаком після коми, `:X` — шістнадцяткове число).

Ці два параметри можна поєднувати. Наприклад, `{price,10:F2}` виведе число `price` з двома знаками після коми, вирівняне праворуч у полі шириною щонайменше 10 символів — зручно, коли потрібна колонка з грошовими сумами, де копійки в усіх рядків мають стояти рівно одна під одною.

Розширений приклад — таблиця з трьома колонками різних типів (текст, ціле число, дробове число з форматом):

```csharp
static void PrintReceipt(string[] itemNames, int[] quantities, double[] prices)
{
    Console.WriteLine($"{"Товар",-18}{"К-сть",7}{"Ціна",10}");
    Console.WriteLine(new string('-', 35));

    for (int i = 0; i < itemNames.Length; i++)
    {
        Console.WriteLine($"{itemNames[i],-18}{quantities[i],7}{prices[i],10:F2}");
    }
}
```

Результат виконання (умовний приклад — вирівнювання колонок видно завдяки моноширинному шрифту консолі):

```
Товар               К-сть      Ціна
-----------------------------------
Хліб                     2      21.50
Молоко                   1      38.90
Цукерки                  3     115.00
```

Той самий підхід (`{ значення , ±ширина : формат }`) працює і поза інтерпольованими рядками — у методі `string.Format(...)` та в `Console.WriteLine("{0,-15}{1,5}", name, score)` з нумерованими плейсхолдерами `{0}`, `{1}` — це називається композитним форматуванням (composite formatting) і є більш «класичним» способом того самого прийому, який широко трапляється в старішому коді C#.

### 4. Розширення передзаповненого набору даних — масив або `List<T>`

**Варіант А — звичайний масив** (розширення через `Array.Resize`):

```csharp
// Дані вже заповнені наперед — не потрібно вводити 5 студентів щоразу при запуску
string[] names = { "Іваненко", "Петренко", "Сидоренко", "Коваль", "Мельник" };
int[] scores = { 85, 42, 91, 58, 76 };

// А цей метод викликається з пункту меню "Додати студента" —
// саме тут і демонструється введення з клавіатури з валідацією
static void AddStudent(ref string[] names, ref int[] scores)
{
    Console.Write("Прізвище нового студента: ");
    string newName = Console.ReadLine() ?? "";

    int newScore = ReadInt("Оцінка (0-100): ", 0, 100);

    Array.Resize(ref names, names.Length + 1);
    Array.Resize(ref scores, scores.Length + 1);

    names[^1] = newName;
    scores[^1] = newScore;
}
```

**Варіант Б — `List<T>`** (той самий сценарій, без `Array.Resize`):

```csharp
List<string> names = new() { "Іваненко", "Петренко", "Сидоренко", "Коваль", "Мельник" };
List<int> scores = new() { 85, 42, 91, 58, 76 };

static void AddStudent(List<string> names, List<int> scores)
{
    Console.Write("Прізвище нового студента: ");
    string newName = Console.ReadLine() ?? "";

    int newScore = ReadInt("Оцінка (0-100): ", 0, 100);

    names.Add(newName);
    scores.Add(newScore);
}
```

Зауважте: `List<T>` — це посилальний тип, тому `AddStudent` змінює список «на місці» й параметр `ref` тут не потрібен (на відміну від масиву у варіанті А, де без `ref` метод отримав би лише копію посилання на *старий* масив і не побачив би результат `Array.Resize`). В іншому елементи `List<T>` перебираються в `foreach` і передаються в методи так само, як масив.

Обидва варіанти одразу показують роботу програми на «живих» даних (набір не порожній під час запуску), а введення з клавіатури й валідацію демонструють саме на додаванні нового елемента, а не на одноманітному введенні всього набору заново щоразу.

### 5. Меню з циклом та `switch expression`

```csharp
static void Main()
{
    bool isRunning = true;

    while (isRunning)
    {
        Console.WriteLine("\n=== Меню ===");
        Console.WriteLine("1. Ввести дані");
        Console.WriteLine("2. Виконати обчислення");
        Console.WriteLine("3. Показати результати");
        Console.WriteLine("0. Вихід");

        int choice = ReadInt("Ваш вибір: ", 0, 3);

        isRunning = choice switch
        {
            1 => HandleInput(),
            2 => HandleCalculation(),
            3 => HandleOutput(),
            0 => false,
            _ => true
        };
    }

    Console.WriteLine("Роботу завершено.");
}
```

### 6. Двовимірний масив (для тих, хто хоче попрактикуватись додатково)

```csharp
static int[,] ReadMatrix(int rows, int cols)
{
    var matrix = new int[rows, cols];

    for (int i = 0; i < rows; i++)
    {
        for (int j = 0; j < cols; j++)
        {
            matrix[i, j] = ReadInt($"Елемент [{i},{j}]: ");
        }
    }

    return matrix;
}
```

## Порядок виконання

1. Створити та налаштувати проєкт (Visual Studio / Rider / VS Code — див. ПР №0), перевірити компіляцію та запуск «порожньої» програми.
2. Спроєктувати структуру меню консольного застосунку: скільки пунктів, що робить кожен пункт, коли завершується цикл.
3. Реалізувати введення даних з валідацією (`TryParse`, цикл повторного введення) — за зразком розділу «Приклади коду» вище.
4. Реалізувати основний алгоритм обробки даних згідно з варіантом, винести логіку у щонайменше 2 окремі методи з параметрами та поверненим значенням.
5. Реалізувати зрозуміле виведення результатів (за бажанням — з вирівнюванням у вигляді таблиці, це не обов'язково) та протестувати на межових і некоректних вхідних даних (порожній масив, нульові/від'ємні значення, невалідний ввід).
6. Оформити код (осмислене іменування змінних і методів, коментарі до нетривіальних ділянок), завантажити у Git-репозиторій.

## Варіанти завдань

Кожен варіант нижче описаний з конкретними вхідними даними, переліком обов'язкових операцій та очікуваним виглядом виводу — щоб під час виконання не залишалось відкритих запитань щодо обсягу роботи.

### Варіант 1. Обробка масиву оцінок студентів

**Вхідні дані:** два паралельні масиви однакової довжини (5–10 елементів) — `string[] names` (ПІБ або прізвище студента) і `int[] scores` (оцінка від 0 до 100). Дозволяється заповнити ці масиви наперед у коді замість введення з клавіатури щоразу; через меню варто передбачити пункт «Додати студента», що розширює масиви за зразком прикладу коду №4 (`Array.Resize`) і саме там демонструє введення з валідацією.

**Обов'язкові операції:**
- обчислити середній бал по всій групі (`CalculateAverage`, метод з п.2 прикладів коду);
- порахувати кількість студентів з оцінкою нижче 60 («незадовільно») та вивести їхні імена;
- відсортувати масив за спаданням оцінки (власний алгоритм сортування — наприклад, сортування вибором або бульбашкою; використання `Array.Sort` з подальшим `Array.Reverse` теж допускається, але тоді додайте власний алгоритм сортування хоча б для одного з інших виводів);
- вивести результат «Прізвище — Бал», відсортований за спаданням (список рядків цілком достатньо; табличне вирівнювання колонок, як у прикладі `PrintTable`, не обов'язкове, але буде плюсом).

**Граничний випадок для обробки:** масив з 0 студентів (порожній ввід) — програма повинна вивести повідомлення «Немає даних» замість спроби ділення на нуль при обчисленні середнього.

### Варіант 2. Калькулятор виразів у консолі

**Вхідні дані:** рядок з простим арифметичним виразом з двома операндами та однією операцією за раз, що вводиться користувачем у форматі `число оператор число` (наприклад, `12 + 7`); підтримка дужок і виразів з кількома операціями **не вимагається** — це спрощений калькулятор, а не повноцінний парсер.

**Обов'язкові операції:**
- розбір введеного рядка на операнди та символ операції (`string.Split` за пробілом);
- підтримка щонайменше 4 операцій: `+`, `-`, `*`, `/`;
- перевірка ділення на нуль з відповідним повідомленням без аварійного завершення програми;
- накопичення історії обчислень за сеанс (масив або список рядків виду `"12 + 7 = 19"`) з можливістю вивести всю історію окремим пунктом меню.

**Граничний випадок для обробки:** ділення на нуль та введення оператора, якого немає в списку підтримуваних (наприклад, `%` або `^`) — вивести повідомлення про непідтримувану операцію.

### Варіант 3. Каса магазину

**Вхідні дані:** масив товарів (3–8 позицій), кожна позиція — назва (`string`), ціна за одиницю (`double`), кількість (`int`). Дозволяється заповнити список товарів наперед у коді (масиви-константи); через меню варто передбачити пункт «Додати товар до чека», що розширює масиви за зразком прикладу коду №4 (`Array.Resize`) і демонструє введення з валідацією.

**Обов'язкові операції:**
- підрахунок повної суми чека (сума `ціна × кількість` по всіх позиціях);
- застосування знижки 10% до чека, якщо сума перевищує задану межу (наприклад, 1000 грн) — межу задати константою на початку програми;
- виведення чека («Назва — Кількість — Ціна — Сума» по кожній позиції) з підсумковим рядком «Разом» (з урахуванням знижки, якщо вона застосована, — окремим рядком показати суму знижки); оформлення у вигляді вирівняної таблиці не обов'язкове, але вітається;
- пошук і виведення найдорожчої позиції чека (за сумою `ціна × кількість`, а не лише за ціною за одиницю).

**Граничний випадок для обробки:** кількість товару, введена як 0 або від'ємне число — не додавати таку позицію до чека і вивести попередження.

### Варіант 4. Конвертер величин

**Вхідні дані:** значення для конвертації (`double`) та вибір напрямку конвертації через пункт меню.

**Обов'язкові операції:**
- підтримка щонайменше 3 пар одиниць виміру на вибір студента (наприклад: км ↔ милі, кг ↔ фунти, °C ↔ °F) — **не валют**, оскільки актуальні курси валют змінюються і не можуть бути «зашиті» в код як стала величина;
- реалізація кожної конвертації окремим методом з параметром (значення) і поверненим значенням (результат), без дублювання формули в кількох місцях коду;
- округлення результату до 2 знаків після коми при виведенні (`Math.Round` або форматування `:F2`);
- виведення результату у форматі «12.5 км = 7.77 милі» (з явним зазначенням одиниць виміру в обидва боки).

**Граничний випадок для обробки:** введення від'ємного значення там, де воно фізично некоректне (наприклад, від'ємна температура за Кельвіном, якщо обрано таку пару, або від'ємна відстань) — вивести попередження, не забороняючи явно коректні від'ємні значення (наприклад, °C може бути від'ємним).

### Варіант 5. Облік температур за тиждень

**Вхідні дані:** масив з 7 елементів (`double`) — температура за кожен день тижня (масив назв днів — «Понеділок», «Вівторок» тощо — можна оголосити константою). Дозволяється заповнити температури наперед у коді; введення з клавіатури з валідацією можна продемонструвати окремим пунктом меню — наприклад, зміна температури обраного дня.

**Обов'язкові операції:**
- обчислення середньої температури за тиждень;
- пошук дня з максимальною та мінімальною температурою (з виведенням назви дня, а не лише індексу);
- визначення «аномальних» днів — тих, де температура виходить за межі діапазону [середня − 5, середня + 5], з виведенням списку таких днів;
- виведення списку «День — Температура» з позначкою (наприклад, зірочкою `*`) біля аномальних днів; оформлення у вигляді вирівняної таблиці не обов'язкове, але вітається.

**Граничний випадок для обробки:** усі 7 температур однакові — середнє дорівнює кожному значенню, аномальних днів немає; програма не повинна помилково позначати жоден день як аномальний.

### Варіант 6. Гра «Вгадай число»

**Вхідні дані:** відсутні на старті — число для вгадування генерується програмою випадково (`Random`, діапазон 1–100), гравець вводить свої спроби.

**Обов'язкові операції:**
- цикл введення спроб із підказкою «більше»/«менше» після кожної невдалої спроби;
- підрахунок кількості спроб до вгадування числа за одну гру;
- обмеження максимальної кількості спроб (наприклад, 10) — якщо гравець не вгадав за цю кількість, гра завершується повідомленням із показом загаданого числа;
- збереження результатів (кількість спроб) кожної зіграної партії за сеанс роботи програми в масиві/списку та виведення статистики (найкращий результат — мінімальна кількість спроб, середня кількість спроб) окремим пунктом меню.

**Граничний випадок для обробки:** перша ж спроба гравця виявляється правильною — статистика має коректно зафіксувати результат «1 спроба», а не пропустити цю партію через відсутність невдалих спроб.

## Вимоги до звіту

Звіт з практичної роботи оформлюється у вигляді документа (PDF/DOCX) та має містити:

- тему, мету та номер варіанта завдання;
- короткий опис реалізованого функціоналу;
- посилання на репозиторій (GitHub/GitLab) з кодом проєкту та історією комітів;
- знімки екрана (консоль) з демонстрацією роботи програми, у т.ч. обробки граничного випадку свого варіанта;
- висновки (2–4 речення) щодо отриманих результатів та труднощів виконання.

## Контрольні питання

1. Чим відрізняються value type та reference type в C#? Наведіть приклади.
2. У чому різниця між switch statement та switch expression?
3. Як коректно обробити некоректне введення користувача без аварійного завершення програми?
4. Які переваги виокремлення логіки в окремі методи порівняно з кодом в Main?
5. Як передаються масиви у методи — за значенням чи за посиланням?
